<?php 
$headMeta = FrontEnd_Helper_viewHelper::getHeadMeta($this);

if(!empty($headMeta)) {
    foreach ($headMeta as $socialMediakey=> $socialMediavalue)
    {
       $this->headMeta()->setProperty($socialMediakey, $socialMediavalue);
    }
}
