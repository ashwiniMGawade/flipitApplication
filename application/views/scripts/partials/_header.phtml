<header id="header">
    <div class="top-panel">
           <div class="container">
                 <span class="slogan"><?php echo FrontEnd_Helper_viewHelper::
                     replaceStringVariable($this->translate('Default header text'));?></span>
                 <a class="menu-account" href="javascript:void(0);">Account menu</a>
             <?php
                  $this->locale = LOCALE != 'en' ? LOCALE  : '';
                  $userMenuLink = $this->locale. '/login/usermenu';
                  echo $this->esi($userMenuLink); ?>
            </div>
    </div>
   <div class="navigation">
         <div class="container">
	<?php 
 if(LOCALE == '') : 
     $logoCssClass = 'kortingscode-logo';
     $siteName = 'kortingscode';      
    else :
    $logoCssClass = 'logo'; 
     $siteName = 'FlipIt';
 endif; 
    ?>  
	    <strong class="<?php echo $logoCssClass; ?>"><a href="<?php echo APPLICATION_PATH; ?>"><?php echo $siteName;?></a></strong>
	    <a href="javascript:void(0);;" class="menu-opener"><span class="glyphicon glyphicon-search"></span></a>
	    <div class="nav-bar">
	         <div class="search-box">
	<?php
    $localePartialPath = APPLICATION_PATH . '/modules/'. LOCALE .'/views/scripts/partials/' ;
    $basePartialViewPath = new Zend_View();
    $basePartialViewPath->setScriptPath(APPLICATION_PATH . '/views/scripts/partials/');
    $localePartialViewPath = new Zend_View();
    $localePartialViewPath->setScriptPath($localePartialPath);                              
 if(file_exists($localePartialPath .  '_headerSearch.phtml')) :
                echo $localePartialViewPath->render('_headerSearch.phtml');
            else:
                echo $basePartialViewPath->render('_headerSearch.phtml');
 endif;
     ?>                          
	 </div>
	   <?php echo FrontEnd_Helper_viewHelper::generateMainMenu($this->controllerName, $this->action); ?>
	</div>
   </div>
  </div>
</header>