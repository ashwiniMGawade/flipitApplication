<header class="heading-box text-shop">
    <h2>
<?php 
echo $this->translate('Similar shops');
?>  
    </h2>
    <strong>
<?php 
echo $this->translate('Check out these similar shops for more discounts');
?>  
    </strong>
</header>
<section class="section">
    <div class="block">
       <div class="holder">
           <ul class="shops">
<?php 
if (!empty($this->similarShopsAndSimilarCategoryShops)) {
    foreach ($this->similarShopsAndSimilarCategoryShops as $shop) {
        $shopLogoName = isset($shop['logo']['name']) ? $shop['logo']['name'] : '';
        $shopLogoPath = isset($shop['logo']['path']) ? $shop['logo']['path'] : '';
        printf(
            '<li><a href="%s"><img alt="%s" src="%s"></a></li>',
            HTTP_PATH_LOCALE  . $shop['permaLink'],
            $shop['name'],
            PUBLIC_PATH_CDN . ltrim($shopLogoPath, "/") . 'thum_small_'. $shopLogoName
        );
    }
}
?>
           </ul>
       </div>
    </div>
</section>