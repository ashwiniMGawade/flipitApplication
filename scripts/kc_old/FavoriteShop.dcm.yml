FavoriteShop:
  type: entity
  table: favorite_shop
  fields:
    id:
      id: true
      type: bigint
      generator:
        strategy: IDENTITY
    shopid:
      type: bigint
      nullable: false
      column: shopId
    createdAt:
      type: datetime
      nullable: false
      column: created_at
    deleted:
      type: integer
      nullable: false
  manyToOne:
    visitorid:
      targetEntity: Visitor
      orphanRemoval: false
      joinColumns:
        visitorId:
          referencedColumnName: id
