MigrationVersion:
  type: entity
  table: migration_version
  fields:
    id:
      id: true
      type: integer
      generator:
        strategy: IDENTITY
    version:
      type: integer
      nullable: true
