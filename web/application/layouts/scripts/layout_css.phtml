<?php
if (APPLICATION_ENV == 'development') {
    $lessCssPath = "/public/css/front_end/";
    $this->headLink(array("rel"=>"stylesheet/less", 'href'=>$lessCssPath.'all.less'));
    echo $this->headLink(array("rel"=>"stylesheet/less", 'href'=>$lessCssPath.'ie.less'));
} else {
    echo $this->headLink()
        ->prependStylesheet("/public/css/front_end/all.css");
}
